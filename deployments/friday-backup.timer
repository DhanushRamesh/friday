# Nightly database backup.
#
#   sudo cp friday-backup.service friday-backup.timer /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now friday-backup.timer

[Unit]
Description=Nightly FRIDAY database backup

[Timer]
OnCalendar=*-*-* 03:30:00
# Runs on the next boot if the machine was off when it was due.
Persistent=true
# Spreads the load if anything else is scheduled at the same minute.
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
